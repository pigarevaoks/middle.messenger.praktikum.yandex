export enum EButtonType {
    Submit = 'submit',
    Button = 'buttom',
    Reset = 'reset'
}

export enum EFieldType {
    Phone = 'phone',
    Email = 'email',
    Password = 'password',
    Text = 'text',
    Message = 'message'
}

export enum EMethod {
    GET = 'GET',
    POST = 'POST',
    PUT = 'PUT',
    DELETE = 'DELETE'
}
