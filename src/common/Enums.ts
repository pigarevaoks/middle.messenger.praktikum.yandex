export enum EButtonType {
    Submit = 'submit',
    Button = 'buttom',
    Reset = 'reset',
}

export enum EFieldsTypes {
    Phone = 'phone',
    Email = 'email',
    Password = 'password',
    Text = 'text',
}
