export enum EButtonType {
    Submit = 'submit',
    Button = 'buttom',
    Reset = 'reset',
}

export enum EFieldType {
    Phone = 'phone',
    Email = 'email',
    Password = 'password',
    DoublePassword = 'double_password',
    Text = 'text',
}
